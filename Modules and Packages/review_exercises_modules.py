'''
1. Create a module called greeter.py that contains a single function
greet() . This function should accept a single string parameter name
and print the text Hello {name}! to the interactive window, with
{name} replaced by the function argument.

2. Create a module called main.py that imports greet() from greeter.py
and calls the function with the argument "Real Python" .
'''
